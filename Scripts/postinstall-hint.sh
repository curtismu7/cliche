#!/bin/bash
# Prints first-run permission steps after install.
cat <<'EOF'

Next: Screen Recording (required for captures)
  1. Open Cliché Settings → Permissions → Enable…
     (or press ⌘⇧6 → Show Permission Prompt)
  2. System Settings → Privacy & Security → Screen & System Audio Recording
     → turn Cliché OFF, then ON
  3. Quit Cliché completely, reopen once, then try ⌘⇧6

EOF
